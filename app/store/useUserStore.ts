// import { create } from "zustand";

// const useUserStore = create((set) => ({
//   user: null,
//   setUser: (user) => set({ user }),
//   clearUser: () => set({ user: null }),
//   bodyType: user.body_type_id,
//   setBodyType: (bodyType) => set({ bodyType }),
// }));

// export default useUserStore;
